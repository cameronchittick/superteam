# superteam 7.1.0 Audit Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superteam:superteam-driven-development (recommended) or superteam:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the nine findings of lead-b's 7.0.0 audit and ship superteam 7.1.0.

**Architecture:** Nine independent edits to the plugin's agents, hooks, skills and manifests, each with its own test. Hooks stay zero-dependency bash (bash 3.2, BSD sed; no `timeout`, no `jq`). Agent behaviour is set by agent frontmatter; the lead's skill text only points at it.

**Tech Stack:** bash, Claude Code plugin manifest (plugins-reference.md), hooks (hooks.md), agent teams (agent-teams.md).

**Spec:** `~/Code/project-lead/docs/superteam/plans/2026-09-06-superteam-7.1.0-audit-brief.md` (read-only; its text is copied verbatim into each task below, so the worktree IC never needs the path).

## Global Constraints

- Zero dependencies: hooks and scripts are bash 3.2 + POSIX tools. No `jq`, no `timeout`/`gtimeout` (absent on the host), no node in hooks.
- Every hook change ships a test under `tests/hooks/` using the `assert_exit` pattern of `tests/hooks/test-team-hooks.sh` (stdin JSON, `env -i PATH=...`, expected exit code). Run tests directly: `bash tests/hooks/<file>.sh`.
- `claude plugin validate .` must pass after every task.
- Agent files keep an explicit `model:` (never `inherit`); an explicit `tools:` allowlist is exact, so team tools stay listed.
- `skills/using-superteam/SKILL.md` stays at most 67 lines (`tests/hooks/test-session-start.sh` asserts it; it is 67 now, so add text inside existing lines).
- Voice: "your human partner", never "the user".
- Keep other-harness support: never remove a manifest or a platform reference.
- Commit messages cite the doc section the change rests on, as `source: <file>#<section>` (e.g. `source: plugins-reference.md#User configuration`). Primary sources: agent-teams.md (When to use agent teams, Size tasks appropriately, Avoid file conflicts), plugins-reference.md (Agents, User configuration, Monitors, File locations reference `bin/`, Plugin manifest schema), hooks.md (PreToolUse decision control).
- Commit trailer on every commit:
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01W77dwNzwuaSbp6LxsaR7WW
  ```
- No `CONTEXT.md` in this repo; use the terms already in the skills (lane, worktree, role tag, Files owned, task graph).

---

### Task 1: Opus defaults via userConfig

**Files owned:** `agents/implementer.md`, `agents/writer.md`, `agents/reviewer.md`, `.claude-plugin/plugin.json`, `tests/claude-code/test-agent-roster.sh`
**Depends on:** none
**Model tier:** most capable

**Spec text (verbatim):** "Models: implementer, writer, reviewer default `model: opus`; researcher, integrator stay sonnet. Expose as `userConfig` `worker_model` and `review_model` with those defaults, substituted into agent content (non-sensitive values only). Update Model Selection to match." (The Model Selection prose edit is Task 7's, which owns the SDD skill file.)

**Source:** plugins-reference.md#User configuration — "Non-sensitive values can also be substituted in skill and agent content" as `${user_config.KEY}`; each option needs `type`, `title`, `description`, optional `default`.

**Interfaces:**
- Produces: `userConfig.worker_model` (default `opus`) used by implementer + writer; `userConfig.review_model` (default `opus`) used by reviewer. Task 7 and Task 8 name these keys in prose.

- [ ] **Step 1: Spike the substitution point (10 min cap).** Add to `agents/implementer.md` frontmatter `model: ${user_config.worker_model}` and to `.claude-plugin/plugin.json`:
  ```json
  "userConfig": {
    "worker_model": {"type": "string", "title": "Worker model", "description": "Model for superteam:implementer and superteam:writer", "default": "opus"},
    "review_model": {"type": "string", "title": "Review model", "description": "Model for superteam:reviewer", "default": "opus"}
  }
  ```
  Run `claude plugin validate .` and `claude -p --plugin-dir . --model sonnet 'Use the Agent tool with subagent_type superteam:implementer and prompt: reply with only the model name you are running as. Print its reply verbatim.'` (this is a subagent spawn, allowed under `-p`).
  - If validate passes and the reply names opus (or the raw `${user_config...}` is nowhere in the output): frontmatter substitution works. Keep it for all three agents.
  - Otherwise: set literal `model: opus` in the three frontmatters and put the substitution in the body only: replace the sentence "The lead may pass a different `model` with a reason; you do not choose it." with "Your default model is `${user_config.worker_model}` (`${user_config.review_model}` for the reviewer), set in the plugin's userConfig; the lead may pass a different `model` with a reason; you do not choose it." Record which branch you took in the report.
- [ ] **Step 2: Failing test.** In `tests/claude-code/test-agent-roster.sh` add a section "roster model defaults": implementer, writer, reviewer frontmatter `model:` is `opus` or `${user_config.worker_model}`/`${user_config.review_model}`; researcher and integrator are `sonnet`; skeptic `opus`; `.claude-plugin/plugin.json` contains `"worker_model"` and `"review_model"` with `"default": "opus"`. Run `bash tests/claude-code/test-agent-roster.sh`; expect the new assertions to FAIL before the edit.
- [ ] **Step 3: Apply to all three agents**, run the test (PASS) and `claude plugin validate .` (pass).
- [ ] **Step 4: Commit** `Agents: opus defaults for implementer, writer, reviewer via userConfig` with `source: plugins-reference.md#User configuration`.

---

### Task 2: Preload skills on every roster agent

**Files owned:** `agents/implementer.md`, `agents/writer.md`, `agents/reviewer.md`, `agents/integrator.md`, `tests/claude-code/test-agent-roster.sh`
**Depends on:** Task 1
**Model tier:** standard

**Spec text (verbatim):** "`skills:` frontmatter: implementer + writer preload test-driven-development and verification-before-completion; reviewer preloads requesting-code-review; integrator preloads finishing-a-development-branch. Prove each loads in a fresh teammate."

**Source:** sub-agents.md#Preload skills into subagents — "The full content of each listed skill is injected into the subagent's context at startup"; plugins-reference.md#Agents lists `skills` among supported plugin-agent fields.

- [ ] **Step 1: Failing test.** Add to `tests/claude-code/test-agent-roster.sh` a "skills preload" section asserting each of the four files has a `skills:` line naming exactly the skills above (plugin-scoped form `superteam:test-driven-development` etc. if Step 3 proves that form loads; bare names otherwise). Run; expect FAIL.
- [ ] **Step 2: Add the frontmatter.** Example for implementer:
  ```yaml
  skills: superteam:test-driven-development, superteam:verification-before-completion
  ```
  Remove from `agents/implementer.md` and `agents/writer.md` the clause "the `skills` field is ignored — invoke skills by name with `Skill`" and replace it with "the `skills` field preloads the skills named above at startup".
- [ ] **Step 3: Prove the load.** For each agent run `claude -p --plugin-dir . --model sonnet 'Use the Agent tool with subagent_type superteam:<role> and prompt: quote the first heading of any skill text already in your context, or say NONE. Print its reply verbatim.'` Expect the skill's H1 (e.g. "Test-Driven Development"). If bare names fail and scoped names work (or vice versa), use the form that works and note it. Paste all four outputs in the report; a teammate uses the same agent file so this is the fresh-load proof the spec asks for.
- [ ] **Step 4: Run** `bash tests/claude-code/test-agent-roster.sh` (PASS), `claude plugin validate .` (pass). Commit `Agents: preload role skills` with `source: sub-agents.md#Preload skills into subagents`.

---

### Task 3: Overlapping Files owned serialize instead of collide

**Files owned:** `skills/writing-plans/SKILL.md`, `hooks/task-created-check`, `tests/hooks/test-team-hooks.sh`, `skills/using-superteam/SKILL.md`, `skills/superteam-driven-development/scripts/task-brief`
**Depends on:** none
**Model tier:** most capable

**Spec text (verbatim):** "Isolation decision: writing-plans adds a `blockedBy` edge when two tasks' `Files owned` overlap; task-created-check rejects an unblocked `Task N:` whose `Files owned` overlap an open task's (exit 2, name the task); using-superteam Step 0 adds 'not a team when: sequential steps, same-file edits, many dependencies' citing agent-teams.md."

**Source:** agent-teams.md#When to use agent teams — "For sequential tasks, same-file edits, or work with many dependencies, a single session or subagents are more effective"; agent-teams.md#Avoid file conflicts; hooks.md#TaskCreated decision control (exit 2 cancels the task).

**Design (the TaskCreated payload has no blockedBy):** the description carries the dependency. `task-brief` emits a `Depends on: Task M, Task K` line (or `Depends on: none`) right after `Files owned:`; the hook exempts an overlap when the overlapping open task's family (`Task M`) is named on the new task's `Depends on:` line. Overlap with no such line stays a rejection naming the task.

- [ ] **Step 1: Failing hook tests** in `tests/hooks/test-team-hooks.sh` (section "task-created-check: Files owned overlap"), using `SUPERTEAM_TASKS_DIR` pointing at a temp dir holding `1.json` = `{"id":"1","subject":"Task 1: implement [implementer]","status":"in_progress","description":"Files owned: a.sh\nDone: x"}`:
  - overlap, no Depends on → exit 2, stderr contains `Task 1: implement`
  - overlap, `Depends on: Task 1` in description → exit 0
  - overlap, `Depends on: Task 2` → exit 2
  - no overlap → exit 0
  Run `bash tests/hooks/test-team-hooks.sh`; expect the second case FAIL.
- [ ] **Step 2: Hook change.** In `hooks/task-created-check`, before the loop: `deps="$(printf '%s\n' "$desc" | sed -n 's/^Depends on:[[:space:]]*//p' | head -1)"`. Inside the loop after computing `$ofam`, add: `printf ',%s,' "$(printf '%s' "$deps" | tr -d ' ')" | grep -q ",$(printf '%s' "$ofam" | tr -d ' ')," && continue`. Update the header comment and `--help` (keep it 5 lines; the test counts them). Tests PASS.
- [ ] **Step 3: task-brief.** In `skills/superteam-driven-development/scripts/task-brief`, parse `**Depends on:**` from the plan task the same way `owned` is parsed and print `Depends on: ${deps}` immediately after the `Files owned:` line for all three kinds. Add the assertion to `tests/hooks/test-team-hooks.sh` (section "task-brief: Depends on line") by running the script on a small fixture plan written to the temp dir; do not edit any test under `tests/claude-code/` (Task 7 owns it).
- [ ] **Step 4: writing-plans.** In the "The three header lines" paragraph of `skills/writing-plans/SKILL.md`, after the sentence ending "collide at merge.", add: "When two tasks must share a file, the later one lists the earlier one in `Depends on:` — that line becomes a `blockedBy` edge on the task list and is what lets the `task-created-check` hook accept the overlap; an overlap with no edge is rejected at creation (agent-teams.md, Avoid file conflicts)."
- [ ] **Step 5: using-superteam Step 0.** Edit the single Step 0 paragraph in `skills/using-superteam/SKILL.md` (line 28) to append, on the same line: " Not a team when the work is sequential steps, same-file edits, or many dependencies — one session or subagents (agent-teams.md, When to use agent teams)." File stays 67 lines; run `bash tests/hooks/test-session-start.sh` (PASS).
- [ ] **Step 6: Commit** `Hooks: Depends on exempts a Files owned overlap; plans wire the edge` with `source: agent-teams.md#Avoid file conflicts` and `source: hooks.md#TaskCreated decision control`.

---

### Task 4: PreToolUse Bash deny hook

**Files owned:** `hooks/bash-guard`, `hooks/hooks.json`, `tests/hooks/test-bash-guard.sh`
**Depends on:** none
**Model tier:** standard

**Spec text (verbatim):** "PreToolUse Bash hook: deny `tmux kill-server`, `git checkout .`, `git reset --hard`, `rm -rf` on a path outside the current worktree. Tests."

**Source:** hooks.md#PreToolUse decision control — return `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}`; "A hook that blocks by exiting 2 routes the same way as deny". Input carries `tool_name` and `tool_input.command`.

**Design:** `hooks/bash-guard` reads stdin JSON, exits 0 unless `tool_name` is `Bash`; extracts `command` with the `json_field`/`json_unescape` helpers copied from `task-created-check`; denies (prints the JSON above to stdout, exit 0) when the command matches, as a whole word sequence anywhere in the string: `tmux kill-server`; `git checkout .` or `git checkout -- .`; `git reset --hard`; `rm -rf <path>` / `rm -fr` / `rm -r -f` where any target argument is absolute, starts with `~`, or contains `..`, and does not resolve under `$PWD` (use `case` on the literal string: absolute paths must start with `"$PWD/"`; `~` and `..` always deny). Everything else exits 0 with no output. `SUPERTEAM_SKIP_VERIFY_GATE=1` bypasses.

- [ ] **Step 1: Failing tests** `tests/hooks/test-bash-guard.sh` (copy the `assert_exit` harness; add an `assert_denied` that greps stdout for `"permissionDecision": "deny"`): each of the four denies; `rm -rf ./build` allowed; `rm -rf "$PWD/tmp"` allowed (pass PWD env); `rm -rf ../other` denied; `rm -rf ~/x` denied; `git checkout main` allowed; `git reset --soft HEAD~1` allowed; non-Bash tool allowed; bypass env allowed; `--help` prints 5 lines.
- [ ] **Step 2: Write the hook**, `chmod +x`, tests PASS.
- [ ] **Step 3: Wire it** in `hooks/hooks.json`:
  ```json
  "PreToolUse": [{"matcher": "Bash", "hooks": [{"type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" bash-guard", "shell": "bash"}]}]
  ```
  Add a wiring assertion in the new test file (python3 json load, `PreToolUse` present with matcher `Bash`). `bash tests/hooks/test-team-hooks.sh` still PASS; `claude plugin validate .` pass.
- [ ] **Step 4: Commit** `Hooks: deny destructive Bash (tmux kill-server, git checkout ., git reset --hard, rm -rf outside the worktree)` with `source: hooks.md#PreToolUse decision control`.

---

### Task 5: Stuck-task monitor

**Files owned:** `monitors/monitors.json`, `hooks/task-monitor`, `tests/hooks/test-task-monitor.sh`
**Depends on:** none
**Model tier:** standard

**Spec text (verbatim):** "Monitor: `monitors/monitors.json` watching the session's task dir; a task in_progress > 20 min with no update emits one line naming it."

**Source:** plugins-reference.md#Monitors — `monitors/monitors.json` is a JSON array of `{name, command, description}`; command runs for the session, each stdout line is a notification; runs only in interactive sessions; `${CLAUDE_PLUGIN_ROOT}` substitutes. agent-teams.md#Team and task files — task list at `~/.claude/tasks/{team-name}/`.

**Design:** `hooks/task-monitor [DIR] [--once]` polls every 60 s (`sleep 60` loop; `--once` runs one pass for tests). DIR defaults to `${SUPERTEAM_TASKS_DIR:-$HOME/.claude/tasks/${CLAUDE_CODE_TASK_LIST_ID:-}}`; with no list id, exit 0 silently. A task is stuck when its JSON has `"status": "in_progress"` and the file mtime is older than `${SUPERTEAM_STUCK_MINUTES:-20}` minutes (`find "$DIR" -name '*.json' -mmin +N`). Emit once per task: `stuck: task <id> "<subject>" in_progress <N> min, owner <owner or none>`; remember emitted ids in `$DIR/.monitor-emitted` and re-emit only if the task's mtime moved past the last emission. Monitor entry:
```json
[{"name": "stuck-tasks", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-hook.cmd task-monitor", "description": "Tasks in_progress with no update for 20 minutes"}]
```

- [ ] **Step 1: Failing tests** `tests/hooks/test-task-monitor.sh`: temp dir with `7.json` in_progress touched `-t` 30 min ago → one line naming task 7 and the subject; second `--once` → no output; `8.json` pending old → no output; `9.json` in_progress fresh → no output; `SUPERTEAM_STUCK_MINUTES=1` with a 2-min-old file → emits; no list id and no DIR → exit 0 no output; `monitors/monitors.json` parses (python3) with name `stuck-tasks`.
- [ ] **Step 2: Write the script and json**, tests PASS, `claude plugin validate .` pass (a validate warning about `experimental.monitors` is acceptable; a load error is not — if validate rejects the top-level file, move the declaration to `"experimental": {"monitors": "./monitors/monitors.json"}` in a report note for Task 8's owner, do not edit plugin.json yourself).
- [ ] **Step 3: Commit** `Monitors: name a task stuck in_progress for 20 minutes` with `source: plugins-reference.md#Monitors`.

---

### Task 6: Rename setup to setup-superteam

**Files owned:** `skills/setup/SKILL.md`, `skills/setup-superteam/SKILL.md`, `hooks/session-start`, `tests/hooks/test-session-start.sh`
**Depends on:** none
**Model tier:** cheap

**Spec text (verbatim):** "Rename skill `setup` → `setup-superteam` (description unchanged)."

- [ ] **Step 1:** `git mv skills/setup skills/setup-superteam`; change `name: setup` to `name: setup-superteam` in its frontmatter; keep `description` byte-identical.
- [ ] **Step 2:** In `hooks/session-start` replace `superteam:setup` with `superteam:setup-superteam` in `setup_notice`; in `tests/hooks/test-session-start.sh` replace the expected string likewise (three occurrences). Run `bash tests/hooks/test-session-start.sh` (PASS). `grep -rn 'superteam:setup\b\|skills/setup/' --exclude-dir=.git .` must return nothing outside README.md (README is Task 8's).
- [ ] **Step 3:** `claude plugin validate .` pass. Commit `Skills: rename setup to setup-superteam`.

---

### Task 7: SDD implementer-seat consistency

**Files owned:** `skills/superteam-driven-development/SKILL.md`, `skills/executing-plans/SKILL.md`, `skills/writing-plans/SKILL.md`, `tests/claude-code/test-superteam-driven-development.sh`
**Depends on:** Task 1, Task 3
**Model tier:** prose, most capable

**Spec text (verbatim):** "SDD consistency: one description of the implementer seat (split-pane teammate + EnterWorktree in team mode; worktree subagent only in fallback). executing-plans description says 'fallback for harnesses without agent teams; on Claude Code use superteam-driven-development'; writing-plans footer recommends SDD only." Plus Task 1's deferred line: "Update Model Selection to match."

- [ ] **Step 1: SDD "Two kinds of IC".** Rewrite the opening so the primary shape is the team-mode seat: implementer and writer are split-pane teammates that claim from the shared list and isolate with `EnterWorktree`; the `isolation: "worktree"` subagent call shape is the fallback-mode dispatch and appears once, under "1. Dispatch the implementer — Fallback mode". Remove the duplicate seat descriptions in "Two kinds of IC", "Modes" and "Role pool" so the seat is described in one place and the others point at it ("see Two kinds of IC"). Keep every rule and template pointer; this is consolidation, not deletion.
- [ ] **Step 2: Model Selection.** Replace "(`sonnet` for implementer, writer, researcher, reviewer and integrator; `opus` for skeptic)" with "(`opus` for implementer, writer, reviewer and skeptic — the first three via the plugin's `worker_model`/`review_model` userConfig, default `opus`; `sonnet` for researcher and integrator)". Adjust "Override up to `opus`" so it no longer lists cases that are now the default (keep the fix-round 4-5 tier-up rule: one tier above the stuck one).
- [ ] **Step 3: Task graph section.** After the task-brief block, add one sentence: "`Depends on:` is printed after `Files owned:`; it is what the `task-created-check` hook reads to accept an overlap with the task it names."
- [ ] **Step 4: executing-plans** frontmatter `description:` becomes: `Fallback for harnesses without agent teams; on Claude Code use superteam-driven-development. Use when you have a written implementation plan to execute alone with review checkpoints`.
- [ ] **Step 5: writing-plans footer.** Replace the two-option "Execution Handoff" with a single handoff: "Plan complete and saved to `docs/superteam/plans/<filename>.md`. Next: superteam:superteam-driven-development (REQUIRED SUB-SKILL). On a harness without agent teams, superteam:executing-plans is the fallback." Update the header banner line 61 the same way (SDD required; executing-plans named as fallback only).
- [ ] **Step 6:** Run `bash tests/claude-code/test-superteam-driven-development.sh` and `bash tests/claude-code/test-dispatch-template.sh`; fix any assertion that pinned the old wording in the file you own (test-superteam-driven-development.sh); if test-dispatch-template.sh breaks, report it — it is not yours. Commit `SDD: one implementer seat; executing-plans is the fallback` with `source: agent-teams.md#When to use agent teams`.

---

### Task 8: Manifest metadata and README

**Files owned:** `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `package.json`, `.codex-plugin/plugin.json`, `.kimi-plugin/plugin.json`, `.cursor-plugin/plugin.json`, `.devin-plugin/plugin.json`, `gemini-extension.json`, `.hermes-plugin/plugin.yaml`, `README.md`
**Depends on:** Task 1, Task 2, Task 4, Task 5, Task 6, Task 9
**Model tier:** cheap

**Spec text (verbatim):** "Manifest: description, keywords, `repository`, `homepage` current."

**Source:** plugins-reference.md#Plugin manifest schema — metadata fields `homepage` (documentation URL), `repository` (source URL), `keywords` (array).

- [ ] **Step 1: Description** on every manifest that has one: `Skills, hooks and an agent roster for Claude Code agent teams: brainstorm, plan, TDD, review and merge as a lead with implementer, reviewer and integrator teammates; other harnesses supported.` Keywords on plugin.json, marketplace.json, package.json (keep `pi-package` where present): `agent-teams, skills, tdd, code-review, planning, worktrees, hooks`.
- [ ] **Step 2:** Add to `.claude-plugin/plugin.json` and `package.json`: `"repository": "https://github.com/cameronchittick/superteam"`, `"homepage": "https://github.com/cameronchittick/superteam#readme"`. Do not touch `version`.
- [ ] **Step 3: README.** Roster lines 337–342: implementer/writer/reviewer say `opus (userConfig worker_model / review_model)`; line 87 and any `superteam:setup` → `superteam:setup-superteam`; hooks section: add `bash-guard` (PreToolUse Bash deny list) and the `stuck-tasks` monitor in one line each; add `bin/superteam-test` under a "Tests" line. Keep the attribution paragraph (line 303) untouched.
- [ ] **Step 4:** `claude plugin validate .`, `bash tests/codex/test-marketplace-manifest.sh`, `bash tests/kimi/run-tests.sh`, `bash tests/devin/test-devin-plugin.sh`, `bash tests/version-bump/test-bump-version.sh` all pass. Commit `Manifests: current description, keywords, repository, homepage; README for 7.1.0` with `source: plugins-reference.md#Plugin manifest schema`.

---

### Task 9: bin/superteam-test runs every suite

**Files owned:** `bin/superteam-test`, `tests/shell-lint/test-lint-shell.sh`
**Depends on:** Task 4, Task 5
**Model tier:** cheap

**Spec text (verbatim):** "Optional if under an hour: `bin/superteam-test` running all suites."

**Source:** plugins-reference.md#File locations reference — `bin/`: "Executables added to the Bash tool's PATH and invokable as bare commands while the plugin is enabled."

- [ ] **Step 1:** Write `bin/superteam-test` (bash, `chmod +x`): resolves the repo root from its own path, runs in order and prints one `PASS`/`FAIL name` line each, exits non-zero if any fail: `tests/hooks/test-team-hooks.sh`, `tests/hooks/test-session-start.sh`, `tests/hooks/test-bash-guard.sh`, `tests/hooks/test-task-monitor.sh`, every `tests/claude-code/test-*.sh` except the integration test (`test-superteam-driven-development-integration.sh` needs `timeout`), `tests/codex/*.sh`, `tests/devin/*.sh`, `tests/kimi/run-tests.sh`, `tests/version-bump/*.sh`, `tests/shell-lint/*.sh`, then `claude plugin validate .` if `claude` is on PATH. `--list` prints the suite names. No `timeout` anywhere.
- [ ] **Step 2:** Make `tests/shell-lint/test-lint-shell.sh` include `bin/` in the files it lints if it enumerates directories. Run `bin/superteam-test`; all PASS.
- [ ] **Step 3:** Commit `bin: superteam-test runs every suite` with `source: plugins-reference.md#File locations reference`.

---

## Dogfood requirement (lead, after merge)

The spec's done line: "dogfood on a 3-task plan with two overlapping-file tasks proves item 3 serializes them". Hooks execute from the installed plugin cache, so the lead runs this after `claude plugin update superteam@cameronchittick` lands 7.1.0, from a fresh `-p` session (TaskCreated fires on `TaskCreate` without a team):

```
CLAUDE_CODE_TASK_LIST_ID=dogfood-710 CLAUDE_CODE_ENABLE_TODO_TOOLS=1 claude -p --plugin-dir . --model sonnet \
 'Create three tasks with TaskCreate, in order, and print each result verbatim: (1) subject "Task 1: implement [implementer]", description "Files owned: a.sh\nDone: x"; (2) subject "Task 2: implement [implementer]", description "Files owned: a.sh\nDepends on: Task 1\nDone: x"; (3) subject "Task 3: implement [implementer]", description "Files owned: a.sh\nDone: x".'
```
Expected: 1 and 2 created, 3 rejected naming `Task 1: implement [implementer]`. Then `TaskUpdate` 2 `addBlockedBy` 1 is the edge writing-plans mandates. Record the output in the ledger and delete the `dogfood-710` list directory.
