{
  "name": "superteam",
  "description": "Skills, hooks and an agent roster for Claude Code agent teams: brainstorm, plan, TDD, review and merge as a lead with implementer, reviewer and integrator teammates; other harnesses supported.",
  "version": "7.0.0",
  "author": {
    "name": "Cameron Chittick"
  },
  "license": "MIT",
  "repository": "https://github.com/cameronchittick/superteam",
  "homepage": "https://github.com/cameronchittick/superteam#readme",
  "keywords": [
    "agent-teams",
    "skills",
    "tdd",
    "code-review",
    "planning",
    "worktrees",
    "hooks"
  ],
  "userConfig": {
    "worker_model": {
      "type": "string",
      "title": "Worker model",
      "description": "Model for superteam:implementer and superteam:writer. Claude Code does not substitute ${user_config.*} into agent frontmatter (verified 2.1.263) — the model is set in agents/*.md; this key records the intent.",
      "default": "opus"
    },
    "review_model": {
      "type": "string",
      "title": "Review model",
      "description": "Model for superteam:reviewer. Claude Code does not substitute ${user_config.*} into agent frontmatter (verified 2.1.263) — the model is set in agents/*.md; this key records the intent.",
      "default": "opus"
    }
  }
}
